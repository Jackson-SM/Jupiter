/* const task_info = document.createElement("div")
const overlay = document.createElement("div")
overlay.classList.add('overlay')

const body_task_info = document.createElement("div")
body_task_info.classList.add('body_task_info')

const top_task_info = document.createElement("div")
top_task_info.classList.add('top_task_info')

const title_task = document.createElement('h3')
title_task.classList.add('title_task')
title_task.textContent = `Título da Task`
const group_task = document.createElement('span')
group_task.classList.add('group_task')
group_task.textContent = `No grupo Backend`
top_task_info.appendChild(title_task)
top_task_info.appendChild(group_task)

const content_task_info = document.createElement("div")
content_task_info.classList.add('content_task_info')

const options_task = document.createElement("div")
options_task.classList.add('options_task')

body_task_info.appendChild(top_task_info)
body_task_info.appendChild(content_task_info)
body_task_info.appendChild(options_task)

task_info.appendChild(overlay)
task_info.appendChild(body_task_info)

document.body.appendChild(task_info); */
